# Logistic Map

Exploring the logistic map!

```
x[n+1] = r * x[n] * (1-x[n])
```

Dependencies:

- Python
- ffmpeg

First, configure the `context.json` parameters to your liking, then
generate the video:

```
make video -i
```

